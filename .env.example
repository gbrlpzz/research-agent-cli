# Environment Configuration for Research Agent

# ============================================================================
# Gemini Authentication (choose one method)
# ============================================================================

# Option 1 (Recommended): Use your Gemini plan's free quota via OAuth
#   Run: research gemini-login
#   This authenticates with Google and uses your Gemini subscription quota.
#   No API key needed - tokens are stored in ~/.gemini-oauth.json

# Option 2: Use API key (billed per token)
# GEMINI_API_KEY=your_gemini_api_key_here

# ============================================================================
# Agent Iteration Limits
# ============================================================================
# Maximum iterations for main research agent (default: 50)
# Increase for complex topics that need more deliberation
AGENT_MAX_ITERATIONS=50

# Maximum iterations for revision phase (default: 25)
REVISION_MAX_ITERATIONS=25

# Maximum iterations for peer reviewer (default: 15)
# Prevents infinite loops during review phase
MAX_REVIEWER_ITERATIONS=15

# ============================================================================
# API Safety Timeouts (seconds)
# ============================================================================
# Timeout for standard API calls (default: 120 seconds / 2 minutes)
API_TIMEOUT_SECONDS=120

# Gemini OAuth Credentials (Required if using Gemini Plan)
# Values can be found in the project documentation or installation guide
GEMINI_CLIENT_ID=
GEMINI_CLIENT_SECRET=

# Antigravity OAuth Credentials (Required if using Antigravity Plan)
ANTIGRAVITY_CLIENT_ID=
ANTIGRAVITY_CLIENT_SECRET=


# Timeout for reviewer API calls (default: 180 seconds / 3 minutes)
# Reviewers need more time for in-depth analysis
REVIEWER_TIMEOUT_SECONDS=180

# ============================================================================
# Optional: Logging
# ============================================================================
# Set to DEBUG for verbose logging, ERROR for minimal logging
# LITELLM_LOG=ERROR
